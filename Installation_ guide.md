# OctoMap Packages (and rviz2):

sudo apt update
sudo apt install -y \
  ros-humble-octomap \
  ros-humble-octomap-msgs \
  ros-humble-octomap-ros \
  ros-humble-octomap-mapping \
  ros-humble-octomap-server \
  ros-humble-rviz2

# Plugins for Rviz2 to visualise 3D map
sudo apt install ros-humble-octomap-rviz-plugins